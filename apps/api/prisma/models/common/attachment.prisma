model Attachment {
  id String @id @default(uuid())

  organizationId String
  organization   Organization @relation(fields: [organizationId], references: [id], onDelete: Restrict)

  // Polymorphic 패턴 — sourceType을 String으로 유지해 새 도메인 추가 시 migration 불필요
  // 유효값 예: "RentalContract" | "SaleOrder" | "ServiceRequest" | "ServiceVisit"
  //           | "Invoice" | "Customer" | "Quotation"
  sourceType String
  sourceId   String

  // 파일 분류 (자유 입력: "계약서", "설치사진", "AS전사진", "입금증" 등)
  category String?

  fileName String
  fileUrl  String
  mimeType String
  fileSize Int    // bytes

  uploadedById String?
  uploadedBy   OrganizationMember? @relation(fields: [uploadedById], references: [id], onDelete: Restrict)

  // Attachment는 불변 기록 — updatedAt 없음
  createdAt DateTime @default(now())

  @@index([organizationId])
  @@index([organizationId, sourceType, sourceId])
  @@index([organizationId, sourceType])
  @@index([uploadedById])
}
