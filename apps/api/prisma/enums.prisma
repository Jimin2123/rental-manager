enum CustomerType {
  INDIVIDUAL // 개인
  BUSINESS // 거래처
}

enum AssetStatus {
  INCOMING // 입고 예정 (발주 후 미도착)
  AVAILABLE // 판매/렌탈 가능
  RENTED // 렌탈 중
  SOLD // 판매 완료
  DISPOSED // 폐기
  LOST // 분실
  UNAVAILABLE // 사용 불가 
}

enum OrderType {
  SALE // 판매
  RENTAL // 렌탈
}

enum OrderStatus {
  REGISTERED // 등록
  CANCELLED // 취소
}

enum VatType {
  NONE // 부가세 없음
  INCLUDED // 부가세 포함
}
