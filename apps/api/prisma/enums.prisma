enum CustomerType {
  INDIVIDUAL // 개인
  BUSINESS // 거래처
}

enum BusinessPartnerRoleType {
  SALES // 매출 거래처
  PURCHASE // 매입 거래처
}

enum AssetStatus {
  INCOMING // 입고 예정 (발주 후 미도착)
  AVAILABLE // 판매/렌탈 가능
  RENTED // 렌탈 중
  SOLD // 판매 완료
  DISPOSED // 폐기
  LOST // 분실
  UNAVAILABLE // 사용 불가 
}

enum OrderType {
  SALE // 판매
  RENTAL // 렌탈
}

enum OrderStatus {
  REGISTERED // 등록
  CANCELLED // 취소
}

enum VatType {
  NONE // 부가세 없음
  INCLUDED // 부가세 포함
}

enum RentalContractStatus {
  ACTIVE // 진행 중
  ENDED // 종료
  CANCELLED // 취소
}

enum RentalBillingStatus {
  SCHEDULED // 예정
  ISSUED // 청구
  PAID // 수납 완료
  CANCELLED // 취소
}

enum RentalBillingItemType {
  MONTHLY_RENT // 월 렌탈료
  DEPOSIT // 보증금
  INSTALLATION // 설치비
  ADJUSTMENT // 조정
}
