enum CustomerType {
  INDIVIDUAL // 개인
  BUSINESS // 거래처
}

enum BusinessPartnerRoleType {
  SALES // 매출 거래처
  PURCHASE // 매입 거래처
}

enum AssetStatus {
  INCOMING // 입고 예정 (발주 후 미도착)
  AVAILABLE // 판매/렌탈 가능
  RENTED // 렌탈 중
  SOLD // 판매 완료
  REPAIR // 수리 중
  DISPOSED // 폐기
  LOST // 분실
  UNAVAILABLE // 사용 불가
}

enum OrderType {
  SALE // 판매
  RENTAL // 렌탈
}

enum OrderStatus {
  REGISTERED // 등록
  CANCELLED // 취소
}

enum VatType {
  NONE // 부가세 없음
  INCLUDED // 부가세 포함
}

enum RentalContractStatus {
  DRAFT    // 작성 중 (장비 미점유)
  ACTIVE   // 진행 중
  ENDED    // 종료
  CANCELLED // 취소
}

enum RentalContractItemStatus {
  PENDING  // 활성화 대기 (계약 DRAFT 상태)
  ACTIVE   // 렌탈 중
  RETURNED // 회수 완료
  REPLACED // 교체됨
  CANCELED // 취소
}

enum BillingTiming {
  PREPAID  // 선불
  POSTPAID // 후불
}

enum AssetEventSourceType {
  RENTAL_CONTRACT      // 렌탈 계약 활성화/종료
  RENTAL_CONTRACT_ITEM // 계약 내 장비 교체/회수
  SALE_ORDER           // 판매
  SERVICE_REQUEST      // AS 접수
  SERVICE_VISIT        // AS 방문 처리
  MANUAL               // 수동 기록
}

// 청구서
enum InvoiceType {
  SALE // 판매 청구
  RENTAL_MONTHLY // 월 렌탈료 청구
  SERVICE_FEE // AS/출장/부품비 청구
  MANUAL // 수동 청구
}

enum InvoiceStatus {
  DRAFT // 작성 중
  ISSUED // 발행됨
  CANCELED // 취소됨
}

enum InvoiceItemType {
  SALE_PRICE // 판매금액
  RENTAL_FEE // 월 렌탈료
  SERVICE_FEE // AS/출장비
  INSTALLATION_FEE // 설치비
  REMOVAL_FEE // 철거비
  DELIVERY_FEE // 배송비
  DEPOSIT // 보증금
  PENALTY // 위약금
  ETC // 기타
}

enum InvoiceAdjustmentType {
  DISCOUNT // 할인
  CANCELLATION // 판매/계약 취소
  RETURN // 반품
  RENTAL_PRORATION // 렌탈 중도 해지 일할 차감
  BILLING_ERROR // 청구 오류 수정
  EXTRA_CHARGE // 추가 청구
  ETC // 기타
}

// 입금
enum PaymentMethod {
  CASH // 현금
  BANK_TRANSFER // 계좌이체
  CARD // 카드
  VIRTUAL_ACCOUNT // 가상계좌
  CMS // CMS 자동이체
  ETC // 기타
}

enum PaymentProvider {
  MANUAL // 수동 등록
  TOSS_PAYMENTS
  PORTONE
  INICIS
  KCP
  BANK_API
  OPEN_BANKING
}

enum PaymentStatus {
  PENDING // 입금 대기
  COMPLETED // 입금 완료
  CANCELED // 취소
  FAILED // 실패
}

// AS
enum ServiceRequestStatus {
  RECEIVED    // 접수
  SCHEDULED   // 방문 예정
  IN_PROGRESS // 처리 중
  COMPLETED   // 완료
  CANCELED    // 취소
}

enum ServiceRequestType {
  REPAIR       // 수리
  MAINTENANCE  // 유지보수
  INSTALLATION // 설치
  REMOVAL      // 철거
  INSPECTION   // 점검
  ETC          // 기타
}

enum ServiceVisitStatus {
  SCHEDULED   // 방문 예정
  IN_PROGRESS // 처리 중
  COMPLETED   // 완료
  CANCELED    // 취소
}

enum ServiceVisitResult {
  REPAIRED        // 수리 완료
  PARTS_REPLACED  // 부품 교체
  CANNOT_REPAIR   // 수리 불가
  DEFERRED        // 재방문 필요
  ETC             // 기타
}

// 환불
enum RefundStatus {
  PENDING // 환불 대기
  COMPLETED // 환불 완료
  FAILED // 실패
  CANCELED // 취소
}

enum RefundReason {
  SALE_CANCEL // 판매 취소
  SALE_RETURN // 반품
  RENTAL_CANCEL // 렌탈 취소
  RENTAL_PRORATION // 중도 해지 일할 환불
  OVERPAYMENT // 초과 입금
  BILLING_ERROR // 청구 오류
  ETC // 기타
}
