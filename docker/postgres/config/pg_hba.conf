# PostgreSQL host-based authentication for the Docker service.
# External network access must still be controlled by Docker host firewall rules.

local all all trust
host all all 127.0.0.1/32 scram-sha-256
host all all ::1/128 scram-sha-256
host all all all scram-sha-256
