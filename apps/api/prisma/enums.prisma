enum CustomerType {
  INDIVIDUAL // 개인
  BUSINESS // 거래처
}

enum AssetStatus {
  INCOMING // 입고 예정 (발주 후 미도착)
  AVAILABLE // 판매/렌탈 가능
  RENTED // 렌탈 중
  SOLD // 판매 완료
  DISPOSED // 폐기
  LOST // 분실
  UNAVAILABLE // 사용 불가 
}
